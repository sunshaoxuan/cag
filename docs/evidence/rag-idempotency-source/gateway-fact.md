# Gateway knowledge fact

The Agent Gateway owns enterprise knowledge, ordered SSE events and capability
governance. Codex performs engineering reasoning through the locally
ChatGPT-authenticated app-server. The default runtime does not use an OpenAI
API key.

Knowledge ingestion is content addressed. Repeating an unchanged source reuses
the existing document, chunks and 1024-dimensional vectors.
