# 0.22.2 测试结果

日期：2026-07-31

## 后端专项

问题中心 9 项测试全部通过。新增覆盖：

* 同一问题连续三轮规划使用不同的事件序号工作区。
* 改进范围为 `backend/app/api/health.py` 时，中文改进内容正常通过。

单文件运行受全仓覆盖率门槛影响返回覆盖率失败，9 项功能断言均通过。

## 后端全量

* 收集 120 项。
* 118 项通过。
* 2 项按 PostgreSQL 环境条件跳过。
* 总覆盖率 85.28%。
* 进程退出码 0。

首次全量运行有 1 项知识调度时序断言偶发失败。该用例随后独立连续运行
10 次全部通过，第二次完整套件全部通过。

## 前端

* 3 个测试文件通过。
* 14 项组件测试通过。
* TypeScript 编译和 Vite 生产构建通过。
* 268 个模块完成转换。

